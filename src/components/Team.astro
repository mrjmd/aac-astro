---
import Icon from './Icon.astro';

const team = [
  { name: "Luc Richard", role: "Founder & Managing Partner", loc: "Connecticut", img: "https://picsum.photos/seed/luc/400/400" },
  { name: "Matt Davis", role: "Managing Partner", loc: "Massachusetts", img: "https://picsum.photos/seed/matt/400/400" },
  { name: "Edward Crowell", role: "Partnership Lead", loc: "Business Development", img: "https://picsum.photos/seed/edward/400/400" },
  { name: "Justin La Fontaine", role: "Lead Technician", loc: "Field Expert", img: "https://picsum.photos/seed/justin/400/400" },
  { name: "Mike Harrington", role: "Technician", loc: "Field Expert", img: "https://picsum.photos/seed/mike/400/400" }
];
---

<section id="about" class="py-24 bg-white relative overflow-hidden">
  <div class="max-w-7xl mx-auto px-6 grid lg:grid-cols-2 gap-16 items-center">
    <div>
      <div class="inline-flex items-center gap-2 px-4 py-2 bg-aac-blue/10 text-aac-blue rounded-full text-[10px] font-black uppercase tracking-widest mb-6 border border-aac-blue/20">
        <Icon name="Users" size={14} /> OUR FAMILY OF FIXERS
      </div>
      <h2 class="text-4xl lg:text-7xl font-display font-black text-aac-dark tracking-tighter leading-none mb-8">
        Talk Directly to <br />
        <span class="text-aac-blue">The Experts.</span>
      </h2>
      <p class="text-lg font-bold text-zinc-500 mb-12 leading-relaxed max-w-lg">
        When you call Attack A Crack, you're speaking directly with the people who understand foundation science. We focus on rapid response and honest diagnosis.
      </p>
      
      <div class="bg-aac-yellow/10 border-l-4 border-aac-yellow p-8 rounded-r-3xl flex gap-4 items-start max-w-lg">
        <div class="w-10 h-10 bg-aac-yellow rounded-xl flex items-center justify-center shrink-0">
          <Icon name="Info" size={20} class="text-aac-dark" />
        </div>
        <p class="text-sm font-bold text-aac-dark italic leading-relaxed">
          "We will talk you through your options including DIY repair if it's something you can handle yourself."
        </p>
      </div>
    </div>
    
    <div class="relative flex flex-col items-center gap-4">
      <!-- Top 2 -->
      <div class="grid grid-cols-2 gap-4 max-w-sm">
        {team.slice(0, 2).map((member) => (
          <div class="bg-white p-4 rounded-3xl border-4 border-aac-dark shadow-[8px_8px_0px_0px_rgba(26,26,26,0.05)]">
            <div class="aspect-square rounded-2xl overflow-hidden mb-4">
              <img src={member.img} alt={member.name} class="w-full h-full object-cover" referrerpolicy="no-referrer" />
            </div>
            <h3 class="font-bold text-aac-dark text-sm">{member.name}</h3>
            <p class="text-aac-blue text-[10px] font-bold uppercase tracking-widest mt-1">{member.role}</p>
            <p class="text-zinc-400 text-[9px] font-bold uppercase tracking-widest mt-1">{member.loc}</p>
          </div>
        ))}
      </div>
      <!-- Bottom 3 -->
      <div class="grid grid-cols-3 gap-4 w-full">
        {team.slice(2).map((member) => (
          <div class="bg-white p-4 rounded-3xl border-4 border-aac-dark shadow-[8px_8px_0px_0px_rgba(26,26,26,0.05)]">
            <div class="aspect-square rounded-2xl overflow-hidden mb-4">
              <img src={member.img} alt={member.name} class="w-full h-full object-cover" referrerpolicy="no-referrer" />
            </div>
            <h3 class="font-bold text-aac-dark text-sm">{member.name}</h3>
            <p class="text-aac-blue text-[10px] font-bold uppercase tracking-widest mt-1">{member.role}</p>
            <p class="text-zinc-400 text-[9px] font-bold uppercase tracking-widest mt-1">{member.loc}</p>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

{
  "name": "attack-a-crack",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev --port=3000 --host=0.0.0.0",
    "build": "astro build",
    "preview": "astro preview",
    "clean": "rm -rf dist",
    "lint": "astro check",
    "validate:schema": "node scripts/validate-schema.js",
    "check:images": "node scripts/check-images.js",
    "check:seo": "node scripts/check-seo.js",
    "validate": "npm run build && npm run validate:schema && npm run check:images && npm run check:seo"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "@tailwindcss/vite": "^4.2.1",
    "astro": "^5.18.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "@lhci/cli": "^0.13.0",
    "@types/node": "^22.14.0",
    "jsdom": "^24.0.0",
    "tailwindcss": "^4.2.1",
    "typescript": "~5.8.2"
  }
}
